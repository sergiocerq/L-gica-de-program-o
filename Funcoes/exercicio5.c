#include <stdio.h>
#include <stdlib.h>
int ler4num();
int main () {
  ler4num(num numero);
  int i;
  for(i=0;i<4;i++) {
    printf("%d", ler4num(num numero);
  }

  
  return 0;
}
int ler4num(num numero) {
  struct num {
    int a[4], i;
  }numero;
  for(i=0;i<4;i++) {
    printf("informe o %d numero", i+1);
    scanf("%d", &a[i]);
    return a[i];
  }
}
